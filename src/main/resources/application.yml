spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/atomsProject?currentSchema=todo
    username: postgres
    password: Shoxruh0912
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    open-in-view: false
  messages:
    always-use-message-format: true
server:
  port: 8081